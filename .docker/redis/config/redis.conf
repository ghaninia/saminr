# Minimal Redis 7 config for local/dev
bind 0.0.0.0
protected-mode no
port 6379

# Persistence
save 900 1
save 300 10
save 60 10000
dir /data

# Append only file for durability
appendonly yes
appendfsync everysec

# Logging
loglevel notice

